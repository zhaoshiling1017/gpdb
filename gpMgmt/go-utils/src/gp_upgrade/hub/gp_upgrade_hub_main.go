package main

import (
	"fmt"
)

// Makefile to build this
// This file's main() to daemonize a gRPC server
// This directory to have the implementation code for the gRPC server to serve
// Needs a logging mechanism
// Minimal CLI command parsing to embrace that booting this binary to run the hub might have some flags

func main() {
	fmt.Println("hello world")
	for i := 0; i < 100; {

	}
}
